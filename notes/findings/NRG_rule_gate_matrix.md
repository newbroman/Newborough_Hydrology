# Project rules and the gates that check them

Generated by `tools/rule_gate_lint.py --write` from `tools/rule_gate_matrix.csv` — edit the CSV, not this file. Rules are CLAUDE.md's and the project box's; gates are what `tools/check_all.sh` runs. `none` and `partial` are the work list.

| enforcement | rules |
|---|---|
| gated | 19 |
| tool-refusal | 4 |
| by-design | 2 |
| partial | 16 |
| procedural | 8 |
| none | 14 |

| id | source | rule | enforcement | gate | note |
|---|---|---|---|---|---|
| R01 | CLAUDE.md §1 | --with-supplementary before every commit (D-102); an opt-in step cannot sit with stale outputs | gated | output_lag |  |
| R02 | CLAUDE.md §1 | Quote the check_all verdict in the commit message; a gate you did not touch failing means stop | procedural |  | commit messages are not linted; nrg_git.sh --ship refuses to commit on a red check_all |
| R03 | CLAUDE.md §3 | Never rewrite a dated record — changelogs and DECISION_LOG entries are appended to; a superseded value gets a banner | none |  | PROPOSED dated_record_lint: fail when the private working tree DELETES lines from a tracked changelog or a dated log entry (wgit diff --numstat deletions > 0 on changelogs/ and DECISION_LOG.md) |
| R04 | CLAUDE.md §3 | DECISIONS_PUBLIC.md is generated from the private DECISION_LOG.md keeping D-numbers | gated | build_public_decisions |  |
| R05 | CLAUDE.md §3 | No Pye & Blott figure | none |  | PROPOSED retired_phrase_lint: 'Pye' in figure ledger and captions |
| R06 | CLAUDE.md §3 | ODTs are edited only through odt_edit.py; never odfpy for writing | partial | odt_media | odt_edit's four guards apply only when it is used; odt_media verify proves each ODT still rebuilds |
| R07 | CLAUDE.md §3 | One document family live at a time; frozen documents take numbers-only writes unless a REASON is named (D-144) | gated | doc_tier_lint |  |
| R08 | CLAUDE.md §3 | The Methods Supplement follows the pipeline by refusal: no registered step without a Supplement chapter and a ledger row | gated | ms_chapters |  |
| R09 | CLAUDE.md §3 | ODTs are versioned; every edit batch writes a bumped filename; mirrors follow the highest NUMERIC version | gated | doc_version_sync refresh_mirrors | ten versions kept per family (D-151) is not checked |
| R10 | CLAUDE.md §3 | Mirrors need pandoc >= 3.0 | tool-refusal | refresh_mirrors |  |
| R11 | CLAUDE.md §3 | Do not send a reader to the Methods Supplement; say the thing or name the script | gated | xref_lint | gated 2026-09-21j; 18 pre-existing pointers await ruling in xref_allow.csv; the Supplement's self-references are advisory |
| R12 | CLAUDE.md §3 | A 0.01 difference at the quoted precision is not a correction | by-design | cite_check | tolerance is built into the drift comparison |
| R13 | CLAUDE.md §3 | export_master_pdf exports JPEG at quality 80 | partial | artefact_lint | the constant is in the tool; no gate reads the PDF's image quality — a sudden size jump is caught only by eye |
| R14 | CLAUDE.md §3 | A stripped (Pictures-less) ODT is a read path only — never committed or placed under report_edits/odt | procedural |  | ODTs are not tracked by git; nothing can detect a stripped copy in place |
| R15 | CLAUDE.md §4 / D-155 | Pipeline runs and PDF builds happen on the publishing machine; the bridge does not produce committable artefacts | gated | env_audit artefact_lint | env_audit fails on the bridge by design; artefact_lint Check C rejects a foreign PDF Producer |
| R16 | CLAUDE.md §4 | Anything handed to Martin that runs more than a few seconds prints progress | procedural |  | no console_utils progress() helper yet; not lintable |
| R17 | CLAUDE.md §4 | Sweep git lock and tmp_obj residue after every bridge git write | tool-refusal |  | working/wgit sweeps; a missed sweep fails the NEXT git command loudly |
| R18 | CLAUDE.md §4 | Never hand-edit docs/PDF_MANIFEST.txt; delete a bad PDF and rerun build_pdfs.sh | partial | export_lag artefact_lint | a hand-edited manifest still certifies a build; only the Producer check sees a foreign one |
| R19 | CLAUDE.md §4 | Do not leave files staged in Martin's tree | none |  | PROPOSED: session_handover --check reports a non-empty public index (git diff --cached --quiet) |
| R20 | CLAUDE.md §4b | Never run refresh_mirrors on a machine whose ODTs were not just pulled; the document lock is taken before editing | tool-refusal | doc_lock | nrg_git.sh 11)/12)/2) refuse or warn; a bridge session can still refresh without the lock |
| R21 | CLAUDE.md §4d | Run repoint_verify after a repoint pass and before hand repair | procedural |  |  |
| R22 | CLAUDE.md §4d | Every generated ledger gets its --check in check_all the day it is written | partial | build_figure_ledger build_provenance_ledger build_value_register | FINDING: build_table_ledger build_doc_ledger and build_number_ledger have no --check and are gated nowhere; build_value_ledger has --check and is gated nowhere |
| R23 | CLAUDE.md §4e | A table cannot have no CSV — every report table traces to a pipeline output | partial | table_source_lint table_gen | FINDING: table_provenance_lint.py which CLAUDE.md names as the enforcer is in neither check_all nor nrg_git |
| R24 | CLAUDE.md §4f | A ship is two pushes — a public commit without its working-repo commit is a change nobody can account for | partial | session_handover | --check fails when the HANDOFF predates the newest public commit; it does not check that the private repo was pushed |
| R25 | CLAUDE.md §5 | Before believing a DRIFTED row read the occurrence; confirmed rows must match the committed CSV | gated | cite_check | --index-only gates confirmed rows; the full triage is advisory |
| R26 | CLAUDE.md §6 | Open work lives in the task register with a check command; a broken check fails | gated | task_lint |  |
| R27 | Project box | Start with HANDOVER_BOOTSTRAP + session_handover; end with --write and a dated HANDOVER_NOTE entry; Tier 0 prose budget binds | gated | session_handover |  |
| R28 | Project box | Every non-trivial methodological call gets a DECISION_LOG entry with Retires and Revisit-if; the handover names the D-numbers | partial | decision_lint session_handover | decision_lint checks entry structure; nothing can tell that a call was made without an entry |
| R29 | Project box | Handovers and changelogs carry a provenance block (model session tree) | partial | session_handover | the generated HANDOFF carries the tree; the hand-written note and changelogs are convention |
| R30 | Project box | Numbers trace to a committed CSV; never cache a value that shifts when scripts rerun | gated | cite_check claims_register | report12's +0.53 survived because it matched no index row — the net covers registered values only |
| R31 | Project box | The pipeline step count is derived never hard-typed; documents quote the manifest | gated | pipeline_count_lint manifest_lint |  |
| R32 | Project box | analytical_phases is not cited in any document | none |  | PROPOSED: a pipeline_count_claims row or a retired_phrase_lint entry for the field name |
| R33 | Project box | 88 dipwells = 66 + 22; 117 counts measuring points not dipwells; never count from wells_clean | partial |  | proof_copy's network tier reads 01_wells_reference/extended; nothing lints '117' or '89' in prose — PROPOSED retired_phrase_lint |
| R34 | Project box | Cluster IDs and labels come from config CLUSTER_LABELS never raw Ward's IDs | none |  | PROPOSED pipeline_lint deps: a script reading 02_07_cluster_membership_k5.csv without importing CLUSTER_LABELS |
| R35 | Project box | Forest management scenarios apply to both C4 and C5 | procedural |  | scientific rule; checkable only per script |
| R36 | Project box | No hardcoded values; constants live in config.py and are imported not mirrored | partial | season_lint defaults_lint drift_term_lint pipeline_lint | pipeline_lint --check literals is ADVISORY (over-reports); E15 SIG_MM and E32 MIN_MONTHS_THRESH are live per-script locals it did not stop |
| R37 | Project box | Rounding is a rendering decision; stores carry full precision; displays show three decimals | partial | rounding_lint store_roundtrip_lint | store-time rounding is gated; display precision is not checked |
| R38 | Project box | record_basis.csv is the register of which record each analysis fits; add a row per analysis | gated | record_basis_lint | CSV vs each script's fit_ssm call |
| R39 | Project box | The report states which record each analysis uses (Martin 2026-09-21: option B — a statement in each methods section) | none |  | PROPOSED record_basis_claims: a fragment per record_basis row that must appear in the named section of the mirror; design pending sign-off |
| R40 | Project box | 03_master_data is the comparison-window store; name the basis before quoting either | procedural |  | part of R39 |
| R41 | Project box | Bucketing day<=15 -> previous month; one implementation; no rainfall lags; every SSM fit uses build_ssm_frame/fit_ssm | partial | month_bucket_lint record_basis_lint | month_bucket_lint covers the bucketing; record_basis_lint reads fit_ssm calls but a reimplemented OLS would not be seen |
| R42 | Project box | Monthly data are referred to by month and year; never YYYY-MM-DD in the report | none |  | PROPOSED retired_phrase_lint: \d{4}-\d{2}-\d{2} in report mirrors with an allowlist for genuinely daily dates (imagery frames) |
| R43 | Project box | Betas are stored positive; a negative cluster-level beta_3 means the datum is wrong (D-007) | partial | claims_register inequality_lint | no claims row binds cluster beta_3 > 0 — PROPOSED row |
| R44 | Project box | Script edits bump __version__ (the assignment) with an inline changelog and the SCRIPT_LEDGER row in the same pass | gated | ledger_lint |  |
| R45 | Project box | A script that emits an artefact is verified by reading the artefact back | procedural |  | artefact_lint checks the file against itself after the fact; the read-back is the author's |
| R46 | Project box | Figure and section numbers are hard-typed; a caption's own number is at its start only | gated | reference_lint figref_lint ref_audit section_ref_audit |  |
| R47 | Project box | Versioned documents are never edited in place; numeric version comparison | gated | doc_version_sync |  |
| R48 | Project box | report.pdf is built by export_master_pdf not build_pdfs; it is outside PDF_MANIFEST and the Producer check | by-design | export_lag |  |
| R49 | Project box | Every ODT is in the drift net through a mirror and DOC_GLOBS; a glob that does not match the filename hides a document | none |  | PROPOSED docglob_lint: every ODT under docs/ and report_edits/odt has a current mirror and every mirror is matched by a DOC_GLOBS pattern — the gap has appeared three times |
| R50 | Project box | Even-handed framing; 'consistent with' not 'confirms' where evidence is incomplete | none |  | PROPOSED advisory wording count ('confirms' 'demonstrates' 'proves') per chapter — 20c found four over-strong uses by hand |
| R51 | Project box | Registered inequality claims bind to committed values | gated | inequality_lint |  |
| R52 | Project box | Do not extrapolate a fitted relationship outside the observed range | procedural |  | scientific rule |
| R53 | Project box | Raw CSVs are read only by Script 01 or a documented exception (09/10/24) | none |  | PROPOSED pipeline_lint deps: a raw data/ path read outside the exception list |
| R54 | Project box | Maps use add_idw_surface make_site_mask add_en_axes; never inline IDW; canonical extent constants | none |  | FINDING: griddata( is called directly in 20_spatial_figures.py (4) and 33_envelope_amplification.py (1) — check whether those are IDW reimplementations; extents are covered by pipeline_lint_literals_allow |
| R55 | Project box | Oxford British spelling; code identifiers and filenames never altered | none |  | PROPOSED advisory spelling pass with identifier exemptions — noisy; low priority |
| R56 | Project box | Never use the em space (U+2003) in any document | partial |  | odt_edit aborts a write containing one; the mirrors themselves are not scanned — PROPOSED retired_phrase_lint entry |
| R57 | Project box | tau is the storage-drainage index not a residence time or drainage timescale; t1/2 is not the aquifer's memory; lambda is quoted by name (D-109) | none |  | PROPOSED retired_phrase_lint: 'residence time' 'drainage timescale' 'used throughout' 'aquifer's memory' 'retains a disturbance' in every mirror |
| R58 | Project box | Two Sy aggregations are named by file; 17_wtf_well_sy.csv does not exist (D-038) | none |  | PROPOSED csv_mention_lint: every *.csv named in a mirror exists under outputs/ or data/ |
| R59 | Project box | A published PDF is built by the publishing machine | gated | artefact_lint |  |
| R60 | Project box | A generated page with relative fetches is published only from the path it assumes | tool-refusal |  | nrg_git.sh stage_web_tools stages exactly the two safe pages |
| R61 | Project box | Never write ODT files through odfpy; edit content.xml as text and rezip; open in headless LibreOffice | partial | odt_edit odt_media | as R06 |
| R62 | Project box | Document images are embedded; deduplication lives in docs/media_store (D-149) | gated | odt_media |  |
| R63 | Project box | Retired numbers must not reappear (1172 ha 8.4 ha 49/46/39 steps 53.15 latitude) | partial | geo_consistency | geo_consistency covers withdrawn GEO values only and is advisory — PROPOSED retired_phrase_lint carries the rest |

## Findings (a tool exists but nothing runs it)

- **R22** — FINDING: build_table_ledger build_doc_ledger and build_number_ledger have no --check and are gated nowhere; build_value_ledger has --check and is gated nowhere
- **R23** — FINDING: table_provenance_lint.py which CLAUDE.md names as the enforcer is in neither check_all nor nrg_git
- **R54** — FINDING: griddata( is called directly in 20_spatial_figures.py (4) and 33_envelope_amplification.py (1) — check whether those are IDW reimplementations; extents are covered by pipeline_lint_literals_allow

## Proposed gates

- **R03** (none) — PROPOSED dated_record_lint: fail when the private working tree DELETES lines from a tracked changelog or a dated log entry (wgit diff --numstat deletions > 0 on changelogs/ and DECISION_LOG.md)
- **R05** (none) — PROPOSED retired_phrase_lint: 'Pye' in figure ledger and captions
- **R19** (none) — PROPOSED: session_handover --check reports a non-empty public index (git diff --cached --quiet)
- **R32** (none) — PROPOSED: a pipeline_count_claims row or a retired_phrase_lint entry for the field name
- **R33** (partial) — proof_copy's network tier reads 01_wells_reference/extended; nothing lints '117' or '89' in prose — PROPOSED retired_phrase_lint
- **R34** (none) — PROPOSED pipeline_lint deps: a script reading 02_07_cluster_membership_k5.csv without importing CLUSTER_LABELS
- **R39** (none) — PROPOSED record_basis_claims: a fragment per record_basis row that must appear in the named section of the mirror; design pending sign-off
- **R42** (none) — PROPOSED retired_phrase_lint: \d{4}-\d{2}-\d{2} in report mirrors with an allowlist for genuinely daily dates (imagery frames)
- **R43** (partial) — no claims row binds cluster beta_3 > 0 — PROPOSED row
- **R49** (none) — PROPOSED docglob_lint: every ODT under docs/ and report_edits/odt has a current mirror and every mirror is matched by a DOC_GLOBS pattern — the gap has appeared three times
- **R50** (none) — PROPOSED advisory wording count ('confirms' 'demonstrates' 'proves') per chapter — 20c found four over-strong uses by hand
- **R53** (none) — PROPOSED pipeline_lint deps: a raw data/ path read outside the exception list
- **R55** (none) — PROPOSED advisory spelling pass with identifier exemptions — noisy; low priority
- **R56** (partial) — odt_edit aborts a write containing one; the mirrors themselves are not scanned — PROPOSED retired_phrase_lint entry
- **R57** (none) — PROPOSED retired_phrase_lint: 'residence time' 'drainage timescale' 'used throughout' 'aquifer's memory' 'retains a disturbance' in every mirror
- **R58** (none) — PROPOSED csv_mention_lint: every *.csv named in a mirror exists under outputs/ or data/
- **R63** (partial) — geo_consistency covers withdrawn GEO values only and is advisory — PROPOSED retired_phrase_lint carries the rest
