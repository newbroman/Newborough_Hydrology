# THIS FILE IS NOT THE PROJECT'S DEPENDENCY LIST. See BOOTSTRAP.md §1.
#
# It is a `pip freeze` from an environment this project has never run in. An
# audit on 2026-08-27 compared it against tools/environment.json — the reference
# environment recorded on the machine that produced every published number — and
# found exactly one line in agreement:
#
#     package        this file      actually installed
#     numpy          2.4.6          1.26.4
#     pandas         3.0.3          2.1.4
#     scipy          1.17.1         1.11.4
#     matplotlib     3.10.9         3.6.3
#     scikit-learn   1.9.0          1.4.1.post1
#     geopandas      1.1.3          0.14.3
#     shapely        2.1.2          2.0.3
#     pyproj         3.7.2          3.6.1
#     statsmodels    0.14.6         0.14.6   <- the only match
#
# Every "actually installed" version above is precisely Ubuntu 24.04's apt
# package. The reference environment is not a pip environment at all: 24.04
# enforces PEP 668 and refuses a bare pip install into the system interpreter,
# which is why. statsmodels is the exception, and that is why it is the one row
# that matches.
#
# `pip install -r requirements.txt` would therefore install numpy 2 and pandas 3
# — two major versions past what produced the published results. The code would
# probably still run, which is the problem rather than the reassurance: it would
# run and quietly give different numbers.
#
# The file is kept rather than deleted because it is evidence of how the
# environment was misdescribed, and because deleting it would only invite
# someone to regenerate it from the wrong machine.
#
# WHAT TO DO INSTEAD
#     BOOTSTRAP.md §1                 the apt line
#     python3 tools/env_audit.py      what is here, against the reference
#     python3 tools/import_audit.py   MISSING-DEP names anything absent
#
# --- the original pip freeze, retained verbatim below this line ---
adjustText==1.4.0
affine==2.4.0
attrs==26.1.0
cairosvg==2.9.0
certifi==2026.5.20
charset-normalizer==3.4.7
click==8.4.1
click-plugins==1.1.1.2
cligj==0.7.2
contextily==1.7.0
contourpy==1.3.3
cycler==0.12.1
fiona==1.10.1
fonttools==4.63.0
geographiclib==2.1
geopandas==1.1.3
geopy==2.4.1
idna==3.18
ImageIO==2.37.3
joblib==1.5.3
kiwisolver==1.5.0
lazy-loader==0.5
matplotlib==3.10.9
mercantile==1.2.1
narwhals==2.22.1
networkx==3.6.1
numpy==2.4.6
odfpy==1.4.1
packaging==26.2
pandas==3.0.3
patsy==1.0.2
pillow==12.2.0
pyogrio==0.12.1
pyparsing==3.3.2
pyproj==3.7.2
python-dateutil==2.9.0.post0
rasterio==1.5.0
requests==2.34.2
scikit-image==0.26.0
scikit-learn==1.9.0
scipy==1.17.1
shapely==2.1.2
six==1.17.0
statsmodels==0.14.6
threadpoolctl==3.6.0
tifffile==2026.6.1
urllib3==2.7.0
xyzservices==2026.3.0
